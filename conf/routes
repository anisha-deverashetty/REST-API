# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

POST     /customer                           controllers.ApplicationController.createCustomer

POST     /invoice                            controllers.ApplicationController.createInvoice

POST     /payment                            controllers.ApplicationController.createPayment

GET		 /customer/:customerId				 controllers.ApplicationController.getCustomer(customerId: String)

# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST     /customer                           controllers.Application.createCustomer

POST     /invoice                            controllers.Application.createInvoice

POST     /payment                            controllers.Application.createPayment

GET		 /customer/:customerId				 controllers.Application.getCustomer(customerId: String)
